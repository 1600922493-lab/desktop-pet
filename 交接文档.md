# 桌宠 Tauri 迁移 — 交接文档

## 一、任务概述

将 Electron 桌面宠物应用迁移到 Tauri v2，目标：**安装包 ≤ 20MB**。

- 原始 Electron 版本：`~/Desktop/桌宠项目/`，打包后 DMG 103MB（太大）
- Tauri 版本：`~/Desktop/桌宠-tauri/`，打包后 DMG **10MB**（达标）

---

## 二、两个项目的位置

| 项目 | 路径 | 状态 |
|------|------|------|
| Electron 原版 | `~/Desktop/桌宠项目/` | 功能完整，但 103MB |
| Tauri 版 | `~/Desktop/桌宠-tauri/` | 编译通过，DMG 10MB，有 bug 待修 |

---

## 三、Tauri 版当前状态

### 已完成
- [x] Rust + Tauri 环境安装（rustc 1.98.1, cargo tauri 2.11.5）
- [x] Tauri 项目创建并编译通过
- [x] DMG 打包成功（10MB）
- [x] 前端代码从 Electron 复制过来（HTML/CSS/JS）
- [x] Rust 后端：drag_window / get_window_bounds / close_app 命令已注册

### 当前 bug（全部存在）
1. **背景不是透明的** — 窗口显示为白色方块，不是透明桌面宠物
2. **拖拽无效** — 拖不动窗口
3. **关闭按钮无效** — 点了没反应
4. **zzZ 字体和原版不同** — WKWebView 和 Chromium 默认字体不同
5. **视频动画效果和原版不同** — Canvas 去黑底/发光层可能有问题

### 根本原因分析

**问题 1（透明背景）**：WKWebView 默认白色背景。`WebviewWindowBuilder::transparent(true)` 和 `background_color(Color(0,0,0,0))` 已设置，但 WKWebView 自身的 webview 层可能还是白色。需要在 WKWebView 层级设置 `_drawsBackground = NO`。

**问题 2+3（拖拽和关闭失效）**：`window.__TAURI__` 可能未注入或 `invoke` 为 null。`withGlobalTauri: true` 已配置，但手动创建的 WebviewWindow 可能没有正确注入全局 API。

**问题 4（字体）**：CSS 已加 `font-family: -apple-system, "PingFang SC"` 但可能不够。

**问题 5（视频）**：WKWebView 对 canvas `getImageData` 在视频帧上有安全限制，可能抛出 SecurityError。已有 try/catch 降级，但降级后就没有去黑底/发光效果了。

---

## 四、当前代码状态（以实际文件为准）

### 文件结构
```
~/Desktop/桌宠-tauri/
├── src-tauri/
│   ├── Cargo.toml          # tauri v2, macos-private-api feature
│   ├── tauri.conf.json      # withGlobalTauri: true, CSP 关闭
│   ├── build.rs
│   ├── capabilities/default.json  # core:default
│   ├── icons/               # 应用图标
│   └── src/lib.rs           # 3 个命令 + 手动创建窗口
├── src/
│   ├── index.html           # 加了一行内联 background:transparent
│   ├── renderer.js          # IPC 轮询 + 交互逻辑
│   ├── styles.css           # 和 Electron 基本一致，zzZ 加了 font-family
│   ├── character.png
│   ├── star.png
│   ├── sparkle.png
│   └── animation.mp4
├── node_modules/@tauri-apps/api/  # 已安装但未使用（改用全局 API）
└── package.json
```

### lib.rs 关键代码
- 手动 `WebviewWindowBuilder` 创建窗口
- `.transparent(true)` + `.background_color(Color(0,0,0,0))` 已设置
- 3 个 command：`drag_window`、`get_window_bounds`、`close_app`
- `set_visible_on_all_workspaces(true)` + `window.show()`

### index.html
- 比 Electron 版多了一行 `<style>html,body{background:transparent!important;...}</style>`

### renderer.js
- 用 `setupIPC()` 轮询 `window.__TAURI__.core.invoke`（50ms 间隔，最多 100 次）
- IPC 就绪后再绑定关闭按钮
- 拖拽/单击/双击逻辑和 Electron 版一致
- Canvas getImageData 加了 try/catch 降级

---

## 五、下一步计划

### 方案 A：修复 WKWebView 透明问题（推荐）

在 Rust 的 setup 中，拿到 WKWebView 的原生对象，设置 `_drawsBackground = NO`：

```rust
// 需要引入 objc 运行时
// 获取 window 的 webview 对象
// 调用 [wkWebView setValue:@NO forKey:@"drawsBackground"]
```

或者安装 `objc` crate：
```toml
[dependencies]
objc = "0.2"
```

然后在 setup 中：
```rust
#[cfg(target_os = "macos")]
unsafe {
    // 获取 WKWebView 实例并设置 drawsBackground = NO
}
```

同时确认 `window.__TAURI__` 注入 — 如果拖拽/关闭还是不工作，考虑用 `app.get_webview_window("main")` 在 conf 中定义窗口（不手动创建），让 Tauri 自动注入全局 API。

### 方案 B：换用 Tauri conf 定义窗口

将窗口配置写入 `tauri.conf.json` 的 `app.windows` 数组（不是顶层），删除 setup 中的手动创建，让 Tauri 框架自动创建窗口。这样可以确保 `withGlobalTauri` 正确注入。

注意：之前试过顶层 `windows` 字段报错，正确位置是在 `app` 对象内部。

### 方案 C：使用 @tauri-apps/api/window 的 startDragging()

拖拽不需要自己写 IPC。Tauri 提供了 `getCurrentWindow().startDragging()` API，可以直接在 JS 中调用，不需要 Rust 端命令。这样即使 `window.__TAURI__` 注入有延迟也不影响拖拽。

---

## 六、绝对不要再踩的坑

### 6.1 架构差异
1. **Tauri ≠ Electron 的简单替换** — Electron 用 Chromium，Tauri 用 WKWebView（macOS）。WKWebView 在透明度、canvas 安全策略、字体渲染上和 Chromium 有本质差异
2. **WKWebView 默认白色背景** — 光设 CSS `background:transparent` 不够，需要在 WKWebView 原生层设置 `_drawsBackground = NO`
3. **`window.__TAURI__` 注入时机** — 手动创建窗口时可能不注入，用 conf 定义窗口更可靠
4. **Canvas getImageData 安全限制** — WKWebView 对视频帧的像素读取可能有 CORS 限制，需要降级方案

### 6.2 Tauri 配置
5. **`windows` 配置的位置** — 在 Tauri v2 中应放在 `app.windows` 数组里（`tauri.conf.json`），不是顶层字段
6. **`withGlobalTauri: true`** — 必须设置，否则 `window.__TAURI__` 不可用
7. **`macOSPrivateApi: true`** — 透明窗口需要此配置
8. **`background_color(Color(0,0,0,0))`** — 仅设 `.transparent(true)` 可能不够，要同时设背景色为全透明

### 6.3 CSS/前端
9. **WKWebView 字体 ≠ Chromium 字体** — 必须显式指定 `font-family`
10. **CSS 文件可能加载失败** — 在 index.html 中加内联 `<style>` 保底
11. **不要用 `crossorigin="anonymous"` 在视频上** — 会导致 canvas tainted，无法读取像素

### 6.4 开发习惯
12. **不要反复重构** — 用户已明确表达不满。修 bug 时做最小改动，不要同时改多个文件
13. **先确认原代码能编译运行，再逐个加修复** — 不要一上来就大改
14. **release 构建验证** — dev 模式的 devtools 可能干扰透明效果，最终用 release 版本测试

---

## 七、Electron 原版功能参考（Tauri 版需要达到同样效果）

- 300x300 透明无边框置顶窗口
- 角色呼吸动画（3s 周期）
- 单击：zzZ 气泡 + 6 颗星星闪烁
- 双击：播放角色动作视频（Canvas 去黑底 + 米白色发光层 + 爱心弹出）
- 拖拽：钟摆摇摆 + 星星视差
- 关闭按钮（hover 显示，红色 hover）
- 打包 DMG 103MB（原版）

---

## 八、环境信息

- macOS Darwin 25.6.0
- Rust 1.98.1
- cargo-tauri 2.11.5
- Tauri v2.11.6
- Tauri 项目路径：`~/Desktop/桌宠-tauri/`
- 构建命令：`cd ~/Desktop/桌宠-tauri && cargo tauri build`
- 产物：`src-tauri/target/release/bundle/dmg/桌宠_1.0.0_x64.dmg`（10MB）
