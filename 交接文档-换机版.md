# 桌面宠物 · 交接文档（换机版）

> 写给在 **Windows 上**接手的新会话。读完这份再动手。
> 最后更新：2026-09-24
> 上一份：`交接文档-新.md`（macOS 时期，结论有对有错，见第三节的"更正"）

---

## ★ 给用户：到 Windows 后怎么开工

**环境准备**：安装 **Rust**、**Node.js**、**Tauri CLI**
```bash
# Rust: https://rustup.rs
# Node: https://nodejs.org
cargo install tauri-cli --version "^2"
```

**拉代码**：
```bash
git clone https://github.com/1600922493-lab/desktop-pet.git
cd desktop-pet
```

**然后对新会话说这句话**（它没有你之前的对话记忆，只能读文件）：

> 先读 `交接文档-换机版.md`，然后我们解决动画黑底问题

**第一件事不是改代码，是跑一遍看黑底还在不在**（见第五节）。

---

## 零、一句话现状

Tauri v2 桌宠，macOS 侧已完成迁移和视觉修复，代码全部推送到 GitHub。
**用户刚换到 Windows，动画黑底问题待验证。**

---

## 一、项目与仓库

```
仓库：https://github.com/1600922493-lab/desktop-pet.git
本地（Windows 上 clone 下来就是）：desktop-pet/
旧版：~/Desktop/桌宠项目/   ← Electron 版（在 Mac 上），仅参考基准，绝不要改
```

**已提交的 commit**（`git log --oneline`）：
```
e12f533 ci: 打 tag 时自动发布 Releases
9544d92 docs: CI 已验证跑通
c40ca7b ci: 改用官方 tauri-action，修正上一步的错误
9ad781b build: 打通 Windows 打包，补充动画备选素材与换机交接文档
3f6446b fix: 修复拖拽瞬间的系统蓝色选区框
879e608 initial
```

**已发布的下载页**：https://github.com/1600922493-lab/desktop-pet/releases
（`v1.0.0` 已发布，含 14MB Windows .exe 和 14.8MB macOS .dmg，公开可下载）

### ★ 需要用户手动拷贝的东西（不在 git 里）

**`~/Desktop/桌宠-动画备选/`（约 37MB）** —— 四个备选素材（2 个 WebP + 2 个 APNG），
内含 `说明.md` 讲怎么挑、怎么换。
没入库是因为 APNG 体积大，真要用时可以用第五节的 ffmpeg 命令几分钟重新生成。
**换机前必须拷 U 盘，否则 Windows 上拿不到。**
（仓库里的 `src/anim-candidates/` 只有两个小体积 WebP，是它的子集）

---

## 二、技术栈与结构

```
src/                    ← 前端（原生 HTML/CSS/JS，无框架）
  index.html
  styles.css
  renderer.js
  character.png         角色立绘（1.8MB）
  star.png              背景星星层（1.8MB）
  sparkle.png           前景小星星（670KB）
  animation.mp4         ★动画源（H.264 黑底，768×768，30fps，4.63s，139帧）
  animation.webm        VP9+alpha，516KB，ALPHA_MODE=1（已验证 alpha 完好）
  anim-candidates/      ★本次新增的备选素材，见第五节
src-tauri/src/lib.rs    ← Rust 后端（仅 4 个 IPC 命令）
src-tauri/tauri.conf.json
.github/workflows/build.yml
```

**lib.rs 只有四个命令**：`drag_window` / `get_window_bounds` / `close_app` / `start_drag`

---

## 三、已完成（macOS 侧验证通过）

| 项目 | 状态 |
|------|------|
| Electron(103MB) → Tauri(17MB) 迁移 | ✅ |
| 静止时半透明方块 | ✅ `.shadow(false)` 修复 |
| 双击动画卡顿 | ✅ 重构为 VP9 alpha 直播，删掉 canvas 逐帧抠图 |
| 双击残留帧 / 动画不完整 / 清晰度 | ✅ 已修复 |
| **拖拽瞬间的蓝色方块** | ✅ **本轮修复，见下** |
| 单击 zzZ + 星星 | ✅ |
| 拖拽形变 `scale(0.95,1.05)` | ✅ 正常 |
| **拖拽瞬移（"抓起来跳一下"）** | ⚠️ 见第四节 —— **用户明确表示喜欢，禁止修** |

### 本轮修复的蓝框（重要）

**现象**：拖拽瞬间整个窗口被涂成淡蓝色矩形（约等于窗口大小，颜色 `(103,129,167)` / `(184,207,240)`，即 macOS 系统强调色）。

**根因**：macOS 在 `mousedown` 时会对元素**启动选区流程**。虽然最终选中内容为空，但选区流程本身触发图层重绘，系统用强调色涂满窗口。

**定位过程**（用逻辑探针，不是像素统计 —— 像素统计在这轮多次误判）：
```
MOUSEDOWN   target=DIV.breathing
SELECTSTART target=DIV.breathing   ← 元凶
SELECTIONCHANGE len=0              ← 内容为空，但仍触发重绘
DRAGSTART   0                      ← 排除"图片原生拖影"
```

**修复**（`src/styles.css`，仅此一处）：
```css
* {
  user-select: none;
  -webkit-user-select: none;
}
```
原来 `user-select: none` 只写在 `html, body` 上，**没覆盖到子元素** —— 而 `SELECTSTART` 的 target 是 `DIV.breathing`。

### 对旧文档的两处更正

**更正 1：旧文档说"AppleScript 读不到无边框窗口位置，会报 -1719"——错的。**
实测可以读：`tell process "desktop-pet" to return position of window 1` 正常工作。（进程名是 `desktop-pet`）

**更正 2：旧文档说 `.background_color(Color(0,0,0,0))` 无害 —— 未经验证。**
Tauri 源码里该 API 同时作用于 window 和 webview 两层。本轮修复蓝框时它并未造成问题，但若 Windows 上出现整窗异色，**这是第一个该怀疑的地方**（删掉即可）。

---

## 四、⚠️ 不要"修"的行为：拖拽瞬移

`renderer.js` 的 `mousemove` 里有 `+ 150`，`lib.rs` 的 `drag_window` 里有 `- 150.0`。

这构成一个已知行为：**在窗口非正中心按下并拖拽时，窗口会瞬移 `(抓取点 − 150)` 像素，最多 150px。**

推导：
```
前端 target = startWin + d + 150
后端 位置  = target - 150
有效位移 = d + (抓取点偏移 - 150)
```

**用户 2026-09-24 明确表示："我喜欢抓起来跳一下的效果，不要动。"**

**所以：这不是 bug，禁止修改。** 它跟蓝框无关（蓝框已由 `user-select` 修复验证）。

---

## 五、★ 黑底问题：下一步要做的第一件事

### 背景

动画播放方案 = VP9 alpha WebM 直接丢给 `<video>` + CSS `drop-shadow` 光晕。

macOS 上 **WKWebView 播放时丢弃 alpha** → 两个现象同源：
1. 黑色底显示出来（黑底）
2. `drop-shadow` 无轮廓可跟随（光晕消失）

**文件本身没问题**：本轮用 `ffmpeg -c:v libvpx-vp9` 显式解码验证，alpha 完好（透明 481911 / 不透明 106328），且 `ALPHA_MODE=1`。

### 关键判断：Windows 上**很可能自动就好了**

| | macOS | Windows |
|---|---|---|
| 引擎 | WKWebView (WebKit) | **WebView2 (Chromium)** |
| VP9 alpha | 丢弃 | Chromium 系通常支持 |

**所以第一步不是改代码，是跑一遍看：**
```
在 Windows 上构建运行 → 双击桌宠触发动画 → 黑底还在吗？
```
- **没了** → 什么都不用改，收工
- **还在** → 用第五节的备选素材换方案

### 备选素材（已生成并验证）

全部 139 帧、alpha 完好、几何一致（768×768 或 480×480，30fps）。

**在仓库里（随 git 走，换机后直接有）** —— `src/anim-candidates/`：

| 文件 | 大小 | 说明 |
|------|------|------|
| `anim-768.webp` | **2.5 MB** | ★ 首选。动图 WebP，WebView2 支持良好 |
| `anim-480.webp` | **1.7 MB** | 更小备选（显示尺寸只有 240px，480 足够） |

**在桌面上（大体积，未入库）** —— `~/Desktop/桌宠-动画备选/`：

| 文件 | 大小 | 说明 |
|------|------|------|
| `anim-768.png` | 21 MB | APNG 备选 |
| `anim-480.png` | 11.9 MB | APNG 小尺寸 |

APNG 之所以不入库：共 33MB 会让每次 clone 都背上；而它们是 WebP 全失败才用的兜底，
用上面那条 ffmpeg 命令几分钟就能重新生成。**换机前记得把 `桌宠-动画备选/` 拷到 U 盘。**

**WebP 比 APNG 小 8 倍多**，优先试 WebP。

生成命令（`colorkey` 从黑底抠出 alpha）：
```bash
ffmpeg -i src/animation.mp4 \
  -vf "colorkey=0x000000:0.28:0.08" \
  -c:v libwebp_anim -lossless 0 -q:v 80 -loop 0 \
  src/anim-candidates/anim-768.webp
```

**换成 WebP 的话，前端要改**：`<video>` → `<img>`，`renderer.js` 里播放控制（`play()` / `onended` / `currentTime`）改为按需显示 + `setTimeout` 控制时长。**用户要求「一次只改一个问题」，先验证格式再改代码。**

### 一个更根本的建议（值得先问用户）

`animation.mp4` 是 **H.264 黑底**，本身没有 alpha —— 所有透明都靠 `colorkey` 现抠。抠图边缘质量（黑边/白边/脏边）是**独立于容器格式**的问题，换 WebP 也照样继承。

用户可以用的工具：**剪映**。若能导出**带 alpha 的逐帧 PNG 序列**或 ProRes 4444，就能绕开 colorkey，得到干净边缘。**换机后值得先问一句。**

---

## 六、Windows 打包（本轮已修好）

已做的三处修改：

**1. `tauri.conf.json`**
```json
"targets": ["dmg", "nsis"],
"icon": [..., "icons/icon.ico"]
```
Tauri 会**按平台自动过滤** targets（源码 `tauri-bundler/src/bundle/settings.rs:1078` 证实：macOS→Dmg、Windows→Nsis），所以一处配置两平台通用，不会互相干扰。macOS 本地已实测不报错。

**2. 新增 `src-tauri/icons/icon.ico`**（6 种尺寸，从 512x512.png 生成）

**3. `.github/workflows/build.yml`**
- 构建用官方 `tauri-apps/tauri-action@v0`（自带 CLI 安装 + 构建）
- 上传步骤加 `if-no-files-found: error`（产物缺失时快速失败，不再静默）
- 新增 `release` job：打 `v*` tag 时自动把双平台安装包发到 Releases 页

**发版方式**（以后更新版本）：
```bash
git tag v1.0.1 && git push origin v1.0.1
```
CI 会自动构建并发布到 https://github.com/1600922493-lab/desktop-pet/releases
（用户无需登录即可下载，这个链接就是分享给别人的链接）

### CI 状态：✅ 已跑通（2026-09-24 验证）

`run 35980293566` 两个 job 全绿，产物真实生成：

| 产物 | 大小 |
|------|------|
| `desktop-pet-windows` (.exe) | **14.0 MB** |
| `desktop-pet-macos` (.dmg) | 14.8 MB |

Windows 包 14MB，满足 <50MB 要求。

**分享下载链接**：https://github.com/1600922493-lab/desktop-pet/releases
（点进去直接下载，**别人不需要 GitHub 账号**）

**已知小瑕疵（用户表示不重要，可忽略）**：
安装包文件名开头的"桌宠"两个中文字在 CI 的 Linux runner 上丢失，
变成 `_1.0.0_x64-setup.exe`。**文件内容完全正常**，只是名字难看。
根因：`tauri.conf.json` 的 `productName` 是中文，而安装包文件名直接取自它
（`tauri-bundler` 的 `PackageSettings.product_name`）。
若要修，把 `productName` 改成 ASCII 即可，但会导致系统里显示名变英文。

**踩坑记录**：一开始用 `taiki-e/install-action` 装 tauri-cli，
但该 action **不支持 `tauri-cli` 这个工具名**，两个 job 都失败。
必须用官方 `tauri-apps/tauri-action@v0`（自带 CLI 安装 + 构建）。

**另一个坑**：`git push` 报 `HTTP2 framing layer` / 连接超时，
执行 `git config http.version HTTP/1.1` 后正常（已写入项目配置）。

### 仍需实测 / 待处理

| 项目 | 状态 |
|------|------|
| Windows 透明窗口 | ❓ 需实测 `.transparent(true)` + `.shadow(false)` 的效果 |
| Windows 上是否还有蓝框 | ❓ WebView2 是 Chromium，选区行为可能与 WKWebView 不同 |
| 拖拽在 Windows 的手感 | ❓ 需实测（原生拖拽 / 瞬移效果） |
| 动画黑底 | ❓ **最优先**，见第五节 |
| 代码签名 | ❓ 未签名会触发 SmartScreen 警告；用户目前接受，正式发布再处理 |

**注意：Windows 包必须在 Windows 上构建**（本机 macOS 构建不出来），这是走 GitHub Actions 的原因。

---

## 七、踩过的坑（不要重踩）

### 7.1 改错项目
用户说的"应用"指**已打包的 .app**，对应源码在 `桌宠-tauri/`。
`~/Desktop/桌宠项目/`（Electron 版）是参考基准，**绝不要动**。

### 7.2 一次只改一个
曾一次改 6 处跨两个项目，用户明确不满。
**本轮教训**：修蓝框时一次改了 3 处（原生拖拽、CSS 选区、`background_color`），结果无法归因，只能回退重做。**务必单独改、单独验证。**

### 7.3 不要用像素统计做视觉判据
本轮为此浪费大量时间：判据写错过两次（`b>=r` 恒成立、把静态壁纸当成蓝框），采样间隔漏掉瞬态帧，区域坐标算错。
**改用逻辑探针**（监听 `dragstart` / `selectstart` 等事件）—— 是确定性的，能直接定位。

### 7.4 `user-select` 不继承到子元素（本轮根因）
写在 `html, body` 上不够，必须写在 `*` 或具体元素上。

### 7.5 `getImageData` 是性能毒药
强制 GPU→CPU 同步回读，每帧一次停顿。任何"每帧都要跑"的场景都不要用。

### 7.6 `mix-blend-mode` 在透明窗口里会画出可见矩形
透明窗口里避免使用。

### 7.7 canvas 合成模式**不能**把黑色转透明
`destination-in` / `source-in` 只作用于 alpha 通道，而 H.264 视频没有 alpha（全 255），一个像素都去不掉。去黑底**必须**靠亮度阈值或 colorkey。

### 7.8 ffmpeg 验证 alpha 必须显式指定解码器
```bash
ffmpeg -c:v libvpx-vp9 -ss 1 -i file.webm -frames:v 1 -pix_fmt rgba out.png
```
不加 `-c:v libvpx-vp9`，默认解码器会丢弃 alpha，给出"文件没有 alpha"的**错误结论**。

### 7.9 ffmpeg 报告 `yuv420p` 不代表 alpha 丢失
VP9 的 alpha 存在独立的 BlockAdditional 层，`ffmpeg -i` 只看主层。**要看 `ALPHA_MODE` 元数据（大写）。**

### 7.10 不要直接访问主项目做实验
本轮做法：`cp -Rc` 克隆到 `/tmp/petdiag/dev-probe/` 改，验证通过再同步回主线。**主线全程零污染。**

---

## 八、可用工具

### ffmpeg（本轮已重下）
```bash
/tmp/ffm-try/ffmpeg      # v9.0.2-tessus
```
**注意 `/tmp` 可能被清理。** 重新下载（macOS）：见旧文档第七节。**Windows 上**直接从 ffmpeg 官网下 Windows 构建即可。

可用编码器：`apng` ✓、`libwebp_anim` ✓、`hevc_videotoolbox` ✓、`libvpx-vp9` ✓
可用滤镜：`colorkey` ✓、`lumakey` ✓

### 构建
```bash
cd 桌宠-tauri && cargo tauri build     # 约 35 秒（首次更久）
```

### 环境
- macOS 侧：macOS 26.6.2 / x86_64，Rust 1.98.1，tauri-cli 2.11.5
- **本机无 Homebrew，无 numpy**（Python 3.9 + PIL 11.3.0）
- Windows 侧环境待确认

---

## 九、当前版本状态

| 项目 | 状态 |
|------|------|
| 静止显示 | ✅ |
| 拖拽移动 | ✅ 顺畅，无蓝框，**保留"跳一下"** |
| 单击互动 | ✅ |
| 双击动画流畅/完整/无残留帧 | ✅ |
| 拖拽形变 | ✅ |
| **动画背景** | ❌ 黑底（macOS WKWebView 丢 alpha）→ **在 Windows 上验证** |
| **光晕效果** | ❌ 同根因 |
| Windows 打包 | ✅ CI 已跑通，14MB；Releases 页可分享 |
| 代码签名 | ⚠️ 未做，安装时会被系统拦一下（用户接受） |
| 安装包文件名 | ⚠️ 中文丢失（用户表示不重要） |

---

## 十、★ 与这位用户协作的方式（重要，别踩）

### 用户的背景与偏好

- **不是程序员**，是设计师/内容创作者。桌面有大量 `L1-1-1-U1` 之类的教材文件夹、PSD、Sketch 文件。
- 所以：**解释要讲人话**，说"蓝框是因为系统把窗口当成被选中的文字"，而不是"WebKit 的 selection 触发 layer repaint"。技术细节可以给，但结论先行。
- 用户会用 **剪映**、**Photoshop**、**Sketch**。可以让他处理素材。

### 沟通方式

- **已预批准全部工具权限**（用户原话："接下来问权限的不要问我了，都批准了"）。
  **不要反复问"可以吗"、"要不要我做"** —— 直接做，做完告诉他结果。
- 但**破坏性操作**（删文件、丢改动、覆盖）仍需先确认。
- **用户会不耐烦**。本轮曾因为把一个小问题说成好像出了新问题，被质问
  "为啥突然又有一堆错误，你到底要干嘛"。
  **教训：报告要分清"阻塞的真问题"和"无害的小瑕疵"**，别把后者渲染成危机。
- 用户原话："你太奇怪了，这样一通下来不知道干嘛搞了一个小时这么小小的bug都没修好。
  问题到底是什么你有搞清楚么" —— **不要用一堆实验绕圈子，先把问题定性清楚再动手。**

### 干活方式（血泪教训）

1. **一次只改一个问题**，等用户验证通过再改下一个。
   本轮修蓝框时一次改了 3 处，结果无法归因，只能回退重做。
2. **不要在主线项目上做实验**。用 `cp -Rc` 克隆到 `/tmp` 再改，验证通过才同步。
3. **改代码前先确认用户指的是哪个目录/哪个应用**（"应用"= 已打包的 .app）。
4. **不要改 `~/Desktop/桌宠项目/`（Electron 版）**，那是用户对比问题的基准。
5. **不要"修"拖拽瞬移**（用户明确说喜欢"跳一下"的效果）。

### 用户最在意的

- 安装包 **< 50MB**（现在 14MB ✓）
- **Windows 能用**（这是换机的全部原因）
- 视觉效果要**好看**（他是设计师，对细节敏感）

---

## 十一、给下一个会话的建议

1. **先读本文件**（Windows 上第一件事），再动手
2. **第一优先级**：Windows 上跑一遍，看**黑底是否自动消失**
   —— WebView2 是 Chromium，很可能直接正常，那就什么都不用改
3. **第二优先级**：若黑底还在，先问用户能否从**剪映导出带 alpha 的源**
   （比换格式更治本）；否则试 `anim-768.webp`
4. **第三优先级**：Windows 透明窗口、拖拽手感、蓝框是否复现
5. **不要碰**：Electron 版、拖拽瞬移（用户喜欢）
6. **报告时**：分清真问题和小瑕疵，别把小问题说成危机
