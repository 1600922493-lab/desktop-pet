# 桌面宠物项目 · 交接文档

> 写给完全没有上下文的会话。请从头读完再动手。
> 最后更新：2026-09-24

---

## 一、项目目标

做一个**跨平台桌面宠物**（Windows + macOS），核心要求：

1. 安装包体积 **< 50MB**
2. **Windows 能正常下载使用**（最终在 GitHub 上发布）
3. 视觉效果：透明无边框窗口，角色悬浮在桌面上，带呼吸动画、点击互动、拖拽移动

---

## 二、项目位置与结构

```
~/Desktop/桌宠项目/          ← Electron 版（旧，已废弃，仅作参考，不要改）
~/Desktop/桌宠-tauri/        ← Tauri 版（当前主线，所有工作在这里）
```

### 桌宠-tauri 结构

```
桌宠-tauri/
├── src/                          ← 前端（HTML/CSS/JS，无框架）
│   ├── index.html
│   ├── styles.css
│   ├── renderer.js
│   ├── character.png             ← 角色立绘（透明 PNG，1.8MB）
│   ├── star.png                  ← 背景星星层（1.8MB）
│   ├── sparkle.png               ← 前景小星星（670KB）
│   ├── animation.mp4             ← 动画源（H.264，黑底，1.3MB）
│   ├── animation.webm            ← 转出来的 VP9+alpha 版（516KB）★见第四节
│   └── animation-h264-backup.mp4 ← 备份
├── src-tauri/                    ← Rust 后端
│   ├── src/lib.rs                ← 窗口创建 + 三个 IPC 命令
│   ├── src/main.rs
│   ├── tauri.conf.json
│   ├── Cargo.toml
│   └── icons/
├── .github/workflows/build.yml   ← GitHub Actions（已配好，未验证）
└── 交接文档.md                    ← 旧文档（内容过时，以本文件为准）
```

**技术栈**：Tauri v2 + 原生 HTML/CSS/JS（无 React/Vue）
**构建产物**：17MB .app / 10MB DMG（远小于 50MB 要求 ✓）

---

## 三、已完成的工作

### 3.1 从 Electron 迁移到 Tauri（已完成）

Electron 版打包出来 103MB，超标。已用 Tauri v2 重写，现在 17MB。
前端代码基本原样复用，只改了 IPC 部分（`ipcRenderer` → Tauri `invoke`）。

### 3.2 已修复的 Bug（均已验证通过）

| # | 问题 | 修复方式 |
|---|------|---------|
| 1 | **静止时角色周围有半透明方块** | `lib.rs` 里加 `.shadow(false)` |
| 2 | **双击动画卡顿** | 确认后改为架构重构（见 3.3） |
| 3 | **双击时先闪出中间某一帧** | 播放前 `pause()` + `currentTime = 0` |
| 4 | **动画播不完整** | 同 2 |
| 5 | **清晰度低、边缘锯齿** | 同 2 |

**关于 #1 的重点**：macOS 会给窗口默认绘制阴影，透明无边框窗口 + 默认阴影 = 一圈柔边浅色矩形。**Electron 会自动关闭透明窗口的阴影，Tauri 不会** —— 这正是 Electron 版从无此问题、Tauri 版一直有的根本原因。修复只需 `.shadow(false)` 一行。

### 3.3 渲染架构重构（已完成，但引出新问题）

**旧架构**：黑底 H.264 视频 + canvas 逐帧抠图
- 每帧要跑 384×384 的 `getImageData` / JS 像素循环 / `putImageData`
- `getImageData` 会强制 GPU→CPU 同步回读，每帧一次停顿 → **卡顿根源**
- 光晕用第二个 canvas + CSS `blur()`，内容每帧变化 → 每帧重算模糊，雪上加霜

**新架构**：带 alpha 通道的 VP9 WebM，直接 `<video>` 播放
- 删除了全部 canvas 抠图代码（`renderFrame` / 两个离屏 canvas / 像素循环）
- 光晕改用 CSS `drop-shadow`（跟随 alpha 轮廓，GPU 原生）
- 清晰度恢复 768×768 无损

重构后 **卡顿彻底消失、无残留帧、动画完整播放、清晰度正常** —— 用户已验证前三点通过。

---

## 四、当前卡在哪（★最重要）

### 问题：VP9 alpha 在 macOS WKWebView 里被丢弃

**现象**：重构后动画出现了黑背景，同时光晕（`drop-shadow`）消失。

**根因**（已诊断确认）：
- `animation.webm` 文件**本身 alpha 完全正常** —— 用 ffmpeg 显式指定 `-c:v libvpx-vp9` 解码验证，得到透明 7498 / 不透明 1634，alpha 完好
- 但 **WKWebView 播放时把 alpha 通道丢掉了**
- alpha 一丢 → 黑底直接显示出来（表现为黑背景）+ `drop-shadow` 无轮廓可跟随（表现为光晕消失）
- **两个现象同一个根因**

**这是平台限制，不是代码问题。改代码绕不过去。**

### 为什么不能靠"换一个没有黑背景的视频"解决

视频要"没有黑背景"只有一种实现方式：**带 alpha 通道**。而当前文件恰好就有 alpha 且完好 —— 问题出在播放器解码环节。换同格式的新视频结果不会变。

---

## 五、下一步计划

已经确认可用的工具（见第七节），生成了候选格式，但**还没做实际验证**。

### 候选方案对比

| 方案 | macOS | Windows | 评价 |
|------|-------|---------|------|
| A. 回到 canvas 抠图 | 可用但卡 | 可用 | 已验证可行，但卡顿问题会回来 |
| B. HEVC + alpha (.mov) | **原生支持** | 不支持 | Apple 平台标准，Windows 播放不了 |
| C. **APNG** | 支持 | 支持 | **推荐**，跨平台一致，alpha 无损 |
| D. 双方案（Mac HEVC / Win WebM） | 各自最优 | 各自最优 | 维护成本高 |

### 推荐路线：方案 C（APNG）

理由：
- **跨平台一致** —— macOS 和 Windows 都原生支持，直接满足 Windows 需求
- **alpha 无损** —— 不会有 VP9 那种"文件正常但播放器丢 alpha"的坑
- **代码最简单** —— 就是一个 `<img>`，连视频解码都不涉及
- 代价：139 帧（4.63s @ 30fps）768×768，预计 8-15MB；若超标可降到 480×480 或降帧率

### 具体执行步骤

1. 用 ffmpeg（第七节路径）生成 APNG：
   ```bash
   /tmp/ffm-try/ffmpeg -i src/animation.mp4 \
     -vf "colorkey=0x000000:0.28:0.08" -f apng src/animation.png
   ```
   注意 `colorkey` 参数需按实际效果微调（`0.28` 是相似度阈值，`0.08` 是混合度）
2. 改 `index.html`：`<video>` → `<img>`，引用 apng
3. 改 `renderer.js`：播放逻辑从 video API 改为动画控制（APNG 会自动循环，需要控制单次播放的话用 CSS 或逐帧）
4. **必须在真实 WKWebView 里截图验证** alpha 是否正常显示（不要只靠文件分析）
5. 验证通过后再处理 Windows

### 备选：如果 APNG 体积超标

- 降分辨率到 480×480（显示尺寸只有 240px，实际够用）
- 降帧率到 15fps
- 或考虑 WebP 动图（`libwebp_anim` 编码器可用，体积比 APNG 小很多，但需验证 WebKit 支持）

### Windows 相关待办

| 项目 | 现状 | 需要做 |
|------|------|--------|
| `tauri.conf.json` targets | 只有 `["dmg"]` | **改为 `["dmg", "nsis"]`** —— 否则 Windows CI 找不到 nsis 包会失败 |
| `.github/workflows/build.yml` | 已写好，未验证 | 验证能跑通 |
| 透明窗口 | `.transparent(true)` + `.shadow(false)` | Windows 上需实测效果 |
| 视频格式 | VP9 WebM | **Windows WebView2 对 VP9 alpha 的支持需实测**，不能假设 |
| 代码签名 | 无 | Windows 未签名会触发 SmartScreen 警告，需决定是否处理 |

**CI 配置已存在**（`.github/workflows/build.yml`），有 `build-windows` 和 `build-macos` 两个 job。
注意：Windows 包**必须在 Windows 上构建**，macOS 本地构建不出来 —— 这是走 GitHub Actions 的原因。

---

## 六、踩过的坑（绝对不要再踩）

### 6.1 改错项目 —— 最严重的错误

用户说的"应用"指的是**已打包的 .app**，而对应的源码在 `桌宠-tauri/`。
**曾错误地去改 `桌宠项目/`（Electron 版），把用户本来正常的版本改坏了，用户明确要求"不要瞎改 ele"。**

**规则**：
- `~/Desktop/桌宠项目/`（Electron 版）**是参考基准，不要动它**。用户会用它对比"哪些问题是 Tauri 独有的"
- 所有修改只在 `~/Desktop/桌宠-tauri/` 进行
- 改之前先确认：用户指的是哪个应用、哪个源码目录、哪个 build

### 6.2 不要一次改多个问题

**曾一次性改了 6 处，还跨两个项目改，导致状态混乱、用户无法判断哪处改动有效。**
用户明确表达过不满："你现在给我改得乱七八糟的"。

**规则**：一次只改一个问题，让用户验证通过后再改下一个。

### 6.3 `getImageData` 是性能毒药

`getImageData` / `putImageData` 会强制 GPU 同步等待，每帧产生一次停顿。它会**打断 GPU 流水线**，是最慢的 Canvas 操作。任何"每帧都要跑"的场景都不要用。

### 6.4 `mix-blend-mode` 在透明窗口里会画出可见矩形

透明窗口（`transparent: true`）里用 `mix-blend-mode`，图层边界会渲染成可见的方块。
**曾是"透明方块"问题的来源之一。** 避免在透明窗口里用 blend mode。

### 6.5 canvas 合成模式**不能**把黑色转成透明

我犯过的错误：以为 `destination-in` / `source-in` 能去掉黑底。
**错。** 这些模式只作用于 alpha 通道，而 H.264 视频没有 alpha 通道（全 255），所以一个像素都去不掉。
更糟的是 `source-in` + `fillRect` 会把**整块画布填成纯白** —— 制造出刺眼的白光方块。

**去黑底必须靠亮度阈值或颜色键生成 alpha，合成模式做不到。**
且要用 `colorkey`（试过可用），不要用 `lumakey`（在 VP9 编码链里 alpha 会丢失）。

### 6.6 ffmpeg 验证 alpha 必须显式指定解码器

用 ffmpeg 检查 WebM alpha 时，**默认解码器会丢弃 alpha**，给出"文件没有 alpha"的错误结论。
必须先确认文件本身是否正常，再判断是不是播放端的问题。

正确验证方式：
```bash
ffmpeg -c:v libvpx-vp9 -ss 1 -i file.webm -frames:v 1 -pix_fmt rgba out.png
```
注意加 `-c:v libvpx-vp9`，否则结论会错。

### 6.7 ffmpeg 报告 `yuv420p` 不代表 alpha 丢失

WebM 里 VP9 的 alpha 存在独立的 BlockAdditional 层，`ffmpeg -i` 只能看到主层。
**`Stream: vp9 ... yuv420p` 是正常输出**，不代表 alpha 有问题。要看 `ALPHA_MODE` 元数据（大写）。

### 6.8 macOS 的签名拦截

从网上下载的 ffmpeg 二进制是未签名的，macOS 26 会直接 SIGKILL（退出码 137）。
- `codesign --force --deep --sign -` **会失败**（strict validation 错误）
- `strip` 也会失败（会把二进制搞坏）
- **正确做法**：用 `curl` 下载 evermeet.cx 的构建（见第七节），`chmod +x` + `xattr -c` 后直接可用

### 6.9 代码签名 / 环境工具

- npm 的 `ffmpeg-static` 包在 Apple Silicon + macOS 26 上**不可用**（x86_64 未签名，被杀）
- 本机**没有 Homebrew**，用户尝试安装时因 URL 换行失败
- 结论：**用第七节的 evermeet 构建，不要折腾 Homebrew**

---

## 七、可用工具（★重要，不要重新下载）

### ffmpeg（已下载，可直接用）

```bash
/tmp/ffm-try/ffmpeg
```

- 版本：ffmpeg 9.0.2-tessus（evermeet.cx 构建，x86_64）
- 已 `chmod +x` + `xattr -c`，**可直接执行**
- 可用编码器：`apng` ✓、`libwebp_anim` ✓、`hevc_videotoolbox` ✓、`prores_ks` ✓、`libvpx-vp9` ✓
- 可用滤镜：`colorkey` ✓、`lumakey` ✓

**注意**：`/tmp` 可能在重启后被清理。如果文件不在了，重新下载：
```bash
cd /tmp && curl -fsSL -o ffm.zip "https://evermeet.cx/ffmpeg/getrelease/zip" \
  && unzip -o ffm.zip && chmod +x ffmpeg && xattr -c ffmpeg
```

### 构建命令

```bash
cd ~/Desktop/桌宠-tauri && cargo tauri build
```
约 35 秒。产物：
- `src-tauri/target/release/bundle/dmg/桌宠_1.0.0_x64.dmg`（10MB）
- `src-tauri/target/release/bundle/macos/桌宠.app`（构建后会被清理）

### 安装到桌面测试

```bash
hdiutil attach <dmg> -quiet && sleep 2
cp -R /Volumes/桌宠/桌宠.app ~/Desktop/桌宠2-修复版.app
hdiutil detach /Volumes/桌宠 -quiet
open ~/Desktop/桌宠2-修复版.app
```

### 截图验证（用于自我检查视觉效果）

```bash
screencapture -x /tmp/shot.png     # 全屏截图
```
配合 Python PIL 分析像素。**注意**：AppleScript 读不到无边框窗口的位置（`get position of window 1` 会报 -1719 错误）。

---

## 八、环境信息

- 机器：macOS 26.6.2，x86_64
- Rust：1.98.1 / cargo 1.98.1（已装好）
- tauri-cli：2.11.5（已装好）
- Node/npm：可用
- **Homebrew：未安装**
- 无 sudo 密码（用户给的密码已泄露在对话记录里，**如果那是真实密码，建议提醒用户修改**）

---

## 九、当前版本状态

| 项目 | 状态 |
|------|------|
| 静止显示 | ✅ 正常（无方块） |
| 拖拽移动 | ⚠️ 拖拽时偶有蓝色方块闪现（**未解决**，曾推测与窗口阴影/重绘有关，`shadow(false)` 后仍存在） |
| 单击互动（zzZ + 星星） | ✅ 正常 |
| 双击动画流畅度 | ✅ 正常 |
| 双击残留帧 | ✅ 已修复 |
| 动画完整性 | ✅ 正常 |
| **动画背景** | ❌ **黑色背景（alpha 被播放器丢弃）—— 当前阻塞点** |
| **光晕效果** | ❌ 消失（同根因） |
| 拖拽前角色形变 | ❌ 重构时丢失，需恢复（原逻辑：`scale(0.95, 1.05)`，在 `mousedown` 时触发） |

### 补充：拖拽蓝框问题

`shadow(false)` 修好了**静止时**的方块，但**拖动瞬间**仍会闪现蓝色方块。
推测与 macOS 在透明窗口移动时的重绘机制有关，尚未定位。
`renderer.js` 中拖拽用的 `scale(0.95, 1.05)` 变换可能触发透明窗口重绘 —— 值得尝试改成 `filter: brightness()` 之类不改变几何的写法。

---

## 十、给下一个会话的建议

1. **先读本文件，再动手**
2. **不要碰 `~/Desktop/桌宠项目/`（Electron 版）**
3. **一次只改一个问题**，让用户验证后再继续
4. **改代码前先确认用户指的是哪个应用/目录**
5. 当前第一优先级：**解决 alpha 显示问题（APNG 方案）**
6. 第二优先级：**恢复拖拽前的角色形变效果**
7. 第三优先级：**定位拖拽蓝框**
8. 最后才做 Windows 打包验证
